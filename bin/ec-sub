#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
export PYTHONPATH="${PROJECT_ROOT}/lib:${PROJECT_ROOT}/tools/ec_sub:${PYTHONPATH:-}"
exec python -m ec_sub "$@"
