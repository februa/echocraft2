[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eca-peak-freq"
version = "0.1.0"
description = "Peak frequency extraction tool for ECHOCRAFT"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["eca_peak_freq"]
