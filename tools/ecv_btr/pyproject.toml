[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecv-btr"
version = "0.1.0"
description = "Bearing-Time Record visualization tool for ECHOCRAFT"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "matplotlib",
]
