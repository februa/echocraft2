[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecv-bl"
version = "0.1.0"
description = "Bearing level visualization tool for ECHOCRAFT"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "matplotlib",
]
