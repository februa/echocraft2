[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ec-propagate"
version = "0.1.0"
description = "Propagation loss tool for ECHOCRAFT"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["ec_propagate"]
