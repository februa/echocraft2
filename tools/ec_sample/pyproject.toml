[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ec-sample"
version = "0.1.0"
description = "Binary sampling tool for ECHOCRAFT"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["ec_sample"]
