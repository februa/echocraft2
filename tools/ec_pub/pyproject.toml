[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ec-pub"
version = "0.1.0"
description = "Binary stream publisher for ECHOCRAFT fan-out"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["ec_pub"]
