[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eca-spectrum"
version = "0.1.0"
description = "Spectrum analysis tool for ECHOCRAFT"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["eca_spectrum"]
