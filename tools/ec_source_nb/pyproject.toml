[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ec-source-nb"
version = "0.1.0"
description = "Narrowband source generation tool for ECHOCRAFT"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["ec_source_nb"]
