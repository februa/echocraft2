[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eca-snr"
version = "0.1.0"
description = "SNR estimation tool for ECHOCRAFT"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["eca_snr"]
